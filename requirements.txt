# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database & Cache
psycopg[binary,pool]>=3.1.0
redis>=5.0.0

# Task Queue
rq>=1.16.0

# Vector Store
qdrant-client==1.11.0

# LLM / LangChain
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.0
langchain-cohere>=0.2.0
langgraph>=0.1.0
langgraph-checkpoint-postgres>=1.0.0
groq>=0.9.0

# Cohere (embeddings + reranker)
cohere>=5.5.0

# Observability
langfuse>=2.0.0

# Document Parsing
pymupdf>=1.24.0           # fitz — text + image extraction
pdfplumber>=0.11.0        # table extraction

# BM25 sparse retrieval
rank-bm25>=0.2.2

# Object Storage (S3-compatible — works for both MinIO and Cloudflare R2)
boto3>=1.34.0

# Rate Limiting
slowapi>=0.1.9

# Multipart support (for file uploads)
python-multipart>=0.0.9

# HTTP client (for frontend)
requests>=2.31.0
# Streamlit UI
streamlit>=1.35.0
